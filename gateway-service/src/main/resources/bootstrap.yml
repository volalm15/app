spring:
  application:
    name: gateway-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: 'config-service'

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

